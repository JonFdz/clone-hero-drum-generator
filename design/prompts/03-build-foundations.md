# Prompt 03 — Build CHDG Foundations

```text
Read design/AGENTS.md, design/DESIGN.md, and design/references/design-token-map.md.

Use Pencil MCP tools to populate the frame named "01 / CHDG Foundations" in design/chdg-ui.pen.

Recreate the current component language from the repository. Do not redesign it.

Create reusable baseline components for:

- button / primary;
- button / secondary;
- button / ghost;
- button / disabled;
- field / input;
- field / select;
- card / standard;
- card / mini;
- status pill / success;
- status pill / warning;
- status pill / danger;
- navigation item / normal;
- navigation item / hover;
- navigation item / active;
- message / warning;
- message / danger.

Requirements:

1. Use Pencil variables wherever possible.
2. Use stable, descriptive layer names.
3. Mark appropriate roots as reusable components.
4. Include visible labels for every component and state.
5. Match the current implementation rather than improving it.
6. Do not create React, Tailwind, or code output.
7. Do not modify files outside design/.

After creating the components:
- inspect the frame layout;
- generate a screenshot of the foundations frame;
- correct clipping, overlaps, or inconsistent spacing;
- save the document;
- summarize any implementation detail that could not be represented faithfully.
```
