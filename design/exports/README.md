# Pencil Exports

> **Issue #89 note (2026-07-18):** Current work contains exactly two complete
> IA alternatives. The historical `direction-c-hybrid.png` example below does
> not authorize a third alternative. Approval Checkpoint 2 selected the bounded
> hybrid responsibility model without treating it as a third complete direction.

Approval Checkpoint 3 approved Foundations V1, the component system, and the
complete 1440 flow. The final package also contains all eight 1024 desktop
adaptations. Standalone PNG export was not required; Pencil MCP screenshots and
layout inspection are the recorded visual evidence.

Store generated previews from `chdg-ui.pen` in this directory.

Examples:

```text
current-app-shell.png
current-source-review.png
direction-a-wizard.png
direction-b-workspace.png
direction-c-hybrid.png
```

## Rules

- Exports are generated artifacts, not design source.
- Do not manually edit exports and then treat them as approved design.
- Include an export in a commit only when it helps review a meaningful design change.
- Use stable, descriptive, lowercase kebab-case filenames.
- Avoid exporting personal or private data.
- Use exact frame names from `design/README.md` when recording an export.
- Never treat a standalone export as stronger evidence than the saved Pencil
  source and its validation records.
