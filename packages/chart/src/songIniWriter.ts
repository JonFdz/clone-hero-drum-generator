export function writeSongIni(options: { name: string; artist: string; charter?: string; songFile?: string }): string { return [`[song]`, `name = ${options.name}`, `artist = ${options.artist}`, `charter = ${options.charter ?? "CHDG"}`, `album =`, `genre =`, `year =`, `song_length =`, `diff_drums = 0`, `preview_start_time = 0`, `icon =`, `loading_phrase = Generated by CHDG`, `song = ${options.songFile ?? "song.ogg"}`, ``].join("\n"); }
