# Reference Docs

This folder contains format notes and inspected-behavior references.

Current files:

- `chart-format-drums.md`: Clone Hero / Moonscraper drum chart encoding notes gathered from real `notes.chart` inspection.

These docs are implementation references, not product requirements.
