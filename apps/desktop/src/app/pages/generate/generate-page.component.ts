import { Component } from "@angular/core";

@Component({
  selector: "chdg-generate-page",
  standalone: true,
  template: `
    <header class="page-header"><p class="eyebrow">Generate</p><h1>Generation placeholder</h1><p>The generation workflow is intentionally deferred. Phase 10 only provides the shell and bridge foundation.</p></header>
    <section class="card"><h2>Future output</h2><p>Clone Hero output will be a local folder containing notes.chart, song.ini, and song.ogg. A .chdg file is the project file, not the song package.</p></section>
  `,
})
export class GeneratePageComponent {}
